# Optional server boundary

This package reserves the future `gavel serve` boundary for a self-hosted HTTP
service. It intentionally contains no server implementation in this phase.
Core, adapters, and the CLI must remain usable without it.
